# Database Configuration
# Using Google Cloud SQL instead of Replit database
# PORT=5001
NODE_ENV=development

# Replit Environment Variables
REPLIT_ENVIRONMENT='true'
REPLIT_URL='http://localhost:5000'

# Permanent Google Cloud SQL Configuration
# These settings ensure we always use Google Cloud SQL
PERMANENT_DB_SETTING=CLOUD_SQL
